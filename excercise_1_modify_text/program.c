#include <windows.h>
#include <stdio.h>

int APIENTRY WinMain(HINSTANCE hInst, HINSTANCE hInstPrev, PSTR cmdline, int cmdshow) {
  MessageBox(NULL, "Dags att hacka lite grand. Modifiera detta meddelande om du kan!", "Hugo is a cat", 0);
  return 0;
}
